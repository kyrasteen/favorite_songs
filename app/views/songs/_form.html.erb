<%= form_for(@song) do |f| %>
  <%= f.text_field :title, placeholder: "title" %>
  <!-- same as input html with type -->
  <%= f.text_field :artist, placeholder: "artist" %>
  <%= f.submit %>
  <% end %>
